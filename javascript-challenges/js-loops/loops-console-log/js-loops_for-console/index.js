console.clear();

for (let i = 0; i <= 100; i++) {
  console.log(i);
}

/* Interne Zählvariable (i): Die Variable i wird als Zähler verwendet und startet bei 0.

Kriterium: Der Loop läuft, solange i kleiner oder gleich 100 ist (i <= 100).

Regel zur Erhöhung: Die Zählvariable i wird nach jedem Durchlauf um 1 erhöht (i++). */
